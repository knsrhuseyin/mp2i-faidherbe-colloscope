{
  "name": "colloscope-api-cloudflare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create colloscope-db",
    "db:migrate": "wrangler d1 migrations apply colloscope-db --remote",
    "check": "node --check src/index.js && node test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
